<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal" android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <android.support.design.widget.TextInputLayout
        android:id="@+id/input_wrap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/count_input"
            android:layout_width="@dimen/count_input_width"
            android:layout_height="@dimen/count_input_height"
            android:singleLine="true"
            android:inputType="number"
            android:textSize="@dimen/text_size"
            android:gravity="center"
            android:labelFor="@+id/input_wrap"/>

    </android.support.design.widget.TextInputLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="@dimen/padding_buttons"
        android:paddingEnd="@dimen/padding_buttons"
        android:layout_marginTop="18dp">

        <Button
            android:id="@+id/count_button_up"
            android:layout_width="@dimen/button_width"
            android:layout_height="@dimen/button_height"
            android:background="@drawable/button_up_selector"/>

        <Button
            android:id="@+id/count_button_down"
            android:layout_width="@dimen/button_width"
            android:layout_height="@dimen/button_height"
            android:layout_marginTop="@dimen/button_down_margin_top"
            android:background="@drawable/button_down_selector"/>

    </LinearLayout>

</LinearLayout>