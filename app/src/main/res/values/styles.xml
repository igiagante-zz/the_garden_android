<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textViewStyle">@style/AppTheme.Widget.TextView</item>
    </style>
    
    <!-- General text -->
    <style name="label" parent="AppTheme.Widget.TextView">
        <item name="android:textColor">@color/label_text_color</item>
        <item name="android:textSize">22sp</item>
    </style>

    <!-- General text -->
    <style name="title" parent="AppTheme.Widget.TextView">
        <item name="android:textColor">@color/text_title_color</item>
        <item name="android:textSize">30sp</item>
    </style>

    <!-- Save Button -->
    <style name="save_button" parent="android:Widget.Holo.Light.Button">
        <item name="android:textColor">@color/text_title_color</item>
        <item name="android:textSize">25sp</item>
        <item name="android:background">@drawable/button_save_background</item>
    </style>

    <!-- Cancel Button -->
    <style name="cancel_button" parent="android:Widget.Holo.Light.Button">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">25sp</item>
        <item name="android:background">@drawable/button_cancel_background</item>
    </style>

</resources>
